import FilteredResultsComponent from './FilteredResults'
import FilteredResultsSkeletonComponent from './FilteredResults.skeleton'

const FilteredResults = Object.assign(FilteredResultsComponent, {
  Skeleton: FilteredResultsSkeletonComponent,
})

export default FilteredResults
